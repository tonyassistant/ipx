---
title: Keybindings
description: Full keyboard reference for ipx v1.
---

# Keybindings

## Navigation

- `j` / `↓` move down
- `k` / `↑` move up
- `v` cycle interface visibility between all, active, and inactive
- `[` / `]` or `Shift+Tab` / `Tab` switch inspector views

## Palette

- `p` or `:` open the palette
- `↑` / `↓` move through suggestions
- `Tab` accept the highlighted suggestion
- `→` fill the highlighted suggestion without moving selection
- `Esc` close the palette
- `Enter` run the typed or selected command

## Actions

- `a` move up in the action list
- `s` move down in the action list
- `Enter` run the highlighted action or confirm a gated action
- `Esc` cancel confirmation

## Session

- `r` refresh
- `q` quit
